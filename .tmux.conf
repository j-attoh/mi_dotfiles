# improve colors
set -g default-terminal 'screen-256color'

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# creating panes and windows in current/same  directory 
# split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}" 
bind c new-window -c "#{pane_current_path}"
unbind '"'
unbind %


# reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.tmux.conf

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Enable mouse mode (tmux 2.1 and above)
set -g mouse on

# act like vim
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# start window numbers at 1 to match keyboard order with tmux order
set -g base-index 1

# start pane numbers at 1 to match keyboard order with tmux order
setw -g pane-base-index 1

bind P paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection     # Begin selection in copy mode.
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle  # Begin selection in copy mode.
bind-key -T copy-mode-vi 'V' send -X select-line
bind-key -T copy-mode-vi 'y' send -X copy-selection      # Yank selection in copy mode.

# status bar config 
set -g status-position bottom 
set -g status-justify left
set -g status-left " "
set -g status-right-style "fg=colour148"
# set -g status-right-style "fg=colour142"
set -g status-style default 
# setw -g window-status-current-style fg=black,bg=white
# setw -g window-status-current-style 'fg=colour051,blink'
# setw -g window-status-current-style 'fg=green,bold,blink'
# setw -g window-status-current-style 'fg=colour001,bold' -- red 
# setw -g window-status-current-style 'fg=colour009,bold'
# setw -g window-status-current-style 'fg=red,bold'
setw -g window-status-current-style 'fg=green,bold'


